<p id="notice"><%= flash_messages %></p>

<h1>Store</h1>

<%= categories_select(@store, path_helper: method(:store_path)) %>

<%= render partial: 'media/cards_display', locals: { media_view: @store } %>
